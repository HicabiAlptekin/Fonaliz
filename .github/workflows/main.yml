name: OtoFon Dinamik Analiz

on:
  push:
    branches:
      - main
  schedule:
    # Her iş günü Türkiye saati ile saat 11:15 (UTC 08:15)
    - cron: '15 8 * * 1-5'
  workflow_dispatch: {}

jobs:
  build-and-analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Depoyu Kopyala
        uses: actions/checkout@v4

      - name: Dizin İçeriğini Listele
        run: |
          echo "📂 Depo içeriği listeleniyor..."
          ls -la

      - name: Python 3.10 Kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Bağımlılıkları Yükle
        run: |
          echo "📦 Bağımlılıklar yükleniyor..."
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            echo "requirements.txt bulundu, bağımlılıklar yükleniyor:"
            cat requirements.txt
            pip install -r requirements.txt
          else
            echo "Hata: requirements.txt bulunamadı!"
            exit 1
          fi

      - name: Adım 1 - Fon Listesi Oluştur
        run: |
          echo "📊 Fon listesi oluşturuluyor..."
          if [ -f tarama_script.py ]; then
            python tarama_script.py weekly 2
          else
            echo "Hata: tarama_script.py bulunamadı!"
            exit 1
          fi

      - name: Adım 2 - Dinamik Analiz Yap
        run: |
          echo "📈 Dinamik analiz başlatılıyor..."
          if [ -f analiz_script.py ]; then
            python analiz_script.py
          else
            echo "Hata: analiz_script.py bulunamadı!"
            exit 1
          fi

      - name: Çıktı Dosyalarını Listele
        run: |
          echo "📄 Üretilen çıktı dosyaları listeleniyor..."
          find . -name "*.xlsx" -o -name "*.txt" || echo "Çıktı dosyası bulunamadı."

      - name: Analiz Sonuçlarını Kaydet
        if: always()  # Önceki adımlar başarısız olsa bile çalışır
        uses: actions/upload-artifact@v4
        with:
          name: analiz-sonuclari-${{ github.run_id }}
          path: |
            *.xlsx
            *.txt
          retention-days: 7
        continue-on-error: true  # Dosya bulunamazsa hata vermez
